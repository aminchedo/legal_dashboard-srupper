name: 🌐 ngrok Live Demo (Fixed & Debugged)

on:
  workflow_dispatch:

jobs:
  ngrok-demo-fixed:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - uses: actions/setup-node@v4
      with:
        node-version: '18'
    
    - name: Pre-flight Checks
      env:
        NGROK_AUTHTOKEN: ${{ secrets.NGROK_AUTHTOKEN }}
      run: |
        echo "🔍 Pre-flight checks..."
        
        # Check authtoken
        if [ -z "$NGROK_AUTHTOKEN" ]; then
          echo "❌ NGROK_AUTHTOKEN not found in secrets"
          echo "Please add the authtoken: 314Xps5bkUs1VE7JE0wk8sX9dtg_2m9fX5nrn3eX7x23kwWNf"
          echo "Go to: Repository Settings → Secrets and variables → Actions → New repository secret"
          echo "Name: NGROK_AUTHTOKEN"
          echo "Value: 314Xps5bkUs1VE7JE0wk8sX9dtg_2m9fX5nrn3eX7x23kwWNf"
          exit 1
        fi
        
        echo "✅ NGROK_AUTHTOKEN found (${#NGROK_AUTHTOKEN} characters)"
        
        # Check directory structure
        [ -d "frontend" ] && echo "✅ Frontend directory exists" || echo "❌ Frontend directory missing"
        [ -d "backend" ] && echo "✅ Backend directory exists" || echo "❌ Backend directory missing"
        [ -f "frontend/package.json" ] && echo "✅ Frontend package.json exists" || echo "❌ Frontend package.json missing"
        [ -f "backend/package.json" ] && echo "✅ Backend package.json exists" || echo "❌ Backend package.json missing"
    
    - name: Install ngrok
      env:
        NGROK_AUTHTOKEN: ${{ secrets.NGROK_AUTHTOKEN }}
      run: |
        echo "📥 Installing ngrok..."
        curl -s https://ngrok-agent.s3.amazonaws.com/ngrok.asc | sudo tee /etc/apt/trusted.gpg.d/ngrok.asc >/dev/null
        echo "deb https://ngrok-agent.s3.amazonaws.com buster main" | sudo tee /etc/apt/sources.list.d/ngrok.list
        sudo apt update && sudo apt install ngrok
        
        echo "🔧 Configuring ngrok with authtoken..."
        ngrok config add-authtoken $NGROK_AUTHTOKEN
        ngrok config check
        echo "✅ ngrok configured successfully"
    
    - name: Setup Dependencies
      run: |
        echo "📦 Installing dependencies..."
        
        # Frontend
        if [ -d "frontend" ] && [ -f "frontend/package.json" ]; then
          echo "Installing frontend dependencies..."
          cd frontend
          npm install || {
            echo "❌ Frontend npm install failed"
            exit 1
          }
          cd ..
          echo "✅ Frontend dependencies installed"
        fi
        
        # Backend  
        if [ -d "backend" ] && [ -f "backend/package.json" ]; then
          echo "Installing backend dependencies..."
          cd backend
          npm install || {
            echo "❌ Backend npm install failed"
            exit 1
          }
          cd ..
          echo "✅ Backend dependencies installed"
        fi
    
    - name: Build and Start Servers
      run: |
        echo "🏗️ Building and starting servers..."
        
        # Start backend
        if [ -d "backend" ] && [ -f "backend/package.json" ]; then
          cd backend
          echo "🚀 Starting backend..."
          npm start > ../backend.log 2>&1 &
          BACKEND_PID=$!
          echo $BACKEND_PID > ../backend.pid
          cd ..
          echo "Backend PID: $BACKEND_PID"
          sleep 8
          
          # Test backend
          curl -f http://localhost:3001/health && echo "✅ Backend health OK" || echo "❌ Backend health failed"
        fi
        
        # Build and start frontend
        if [ -d "frontend" ] && [ -f "frontend/package.json" ]; then
          cd frontend
          echo "🏗️ Building frontend..."
          npm run build || {
            echo "❌ Frontend build failed"
            exit 1
          }
          
          echo "🌐 Starting frontend..."
          npm run preview --port 5173 > ../frontend.log 2>&1 &
          FRONTEND_PID=$!
          echo $FRONTEND_PID > ../frontend.pid
          cd ..
          echo "Frontend PID: $FRONTEND_PID"
          sleep 10
          
          # Test frontend
          curl -f http://localhost:5173 && echo "✅ Frontend OK" || echo "❌ Frontend failed"
        fi
    
    - name: Create ngrok Tunnel
      run: |
        echo "🌐 Creating ngrok tunnel..."
        
        # Start ngrok
        ngrok http 5173 --log=stdout > ngrok.log &
        NGROK_PID=$!
        echo $NGROK_PID > ngrok.pid
        echo "ngrok PID: $NGROK_PID"
        
        # Wait for tunnel to establish
        sleep 10
        
        # Extract URL using multiple methods
        echo "🔍 Extracting ngrok URL..."
        
        # Method 1: API
        NGROK_URL=$(curl -s localhost:4040/api/tunnels | jq -r '.tunnels[0].public_url' 2>/dev/null)
        
        # Method 2: Log parsing
        if [ -z "$NGROK_URL" ] || [ "$NGROK_URL" = "null" ]; then
          NGROK_URL=$(grep -o 'https://[^[:space:]]*\.ngrok-free\.app' ngrok.log | head -1)
        fi
        
        # Method 3: Alternative pattern
        if [ -z "$NGROK_URL" ]; then
          NGROK_URL=$(grep -o 'https://[^[:space:]]*\.ngrok\.io' ngrok.log | head -1)
        fi
        
        echo "🌐 Detected ngrok URL: $NGROK_URL"
        
        if [ -z "$NGROK_URL" ]; then
          echo "❌ Failed to extract ngrok URL"
          echo "📋 ngrok log contents:"
          cat ngrok.log
          echo "📋 ngrok API response:"
          curl -s localhost:4040/api/tunnels | jq . || echo "API call failed"
          exit 1
        fi
        
        # Save URL for artifacts
        echo "$NGROK_URL" > ngrok-url.txt
        
        # Test URL
        sleep 5
        curl -I "$NGROK_URL" && echo "✅ ngrok tunnel accessible" || echo "⚠️ ngrok tunnel may need time to become accessible"
    
    - name: Create Demo Report
      run: |
        # Read the ngrok URL
        NGROK_URL=$(cat ngrok-url.txt 2>/dev/null || echo "URL_NOT_FOUND")
        
        cat > ngrok-live-demo-report.html << EOF
        <!DOCTYPE html>
        <html>
        <head>
            <title>🎬 Legal Dashboard - ngrok Live Demo</title>
            <style>
                body { font-family: Arial, sans-serif; margin: 40px; background: #f5f5f5; }
                .container { max-width: 1200px; margin: 0 auto; background: white; padding: 30px; border-radius: 10px; }
                .live-url { background: #28a745; color: white; padding: 20px; border-radius: 8px; text-align: center; margin: 20px 0; }
                .live-url a { color: white; text-decoration: none; font-size: 20px; font-weight: bold; }
                .demo-frame { width: 100%; height: 800px; border: 2px solid #ddd; border-radius: 8px; }
                h1 { color: #333; text-align: center; }
                .status { background: #e8f5e8; padding: 15px; border-radius: 5px; margin: 15px 0; }
            </style>
        </head>
        <body>
            <div class="container">
                <h1>🎬 Legal Dashboard Live Demo</h1>
                
                <div class="live-url">
                    <p>🌐 <strong>LIVE DASHBOARD URL:</strong></p>
                    <a href="$NGROK_URL" target="_blank">$NGROK_URL</a>
                    <p><small>⏰ Available for 2 hours via ngrok</small></p>
                </div>
                
                <div class="status">
                    <h3>✅ Demo Status:</h3>
                    <p>• Backend: Online</p>
                    <p>• Frontend: Online</p>
                    <p>• ngrok Tunnel: Active</p>
                    <p>• Generated: $(date)</p>
                </div>
                
                <h2>📱 Live Dashboard:</h2>
                <iframe src="$NGROK_URL" class="demo-frame"></iframe>
            </div>
        </body>
        </html>
        EOF
        
        echo "✅ Demo report created"
    
    - name: Keep Demo Alive
      run: |
        echo "⏰ Keeping demo alive for 30 minutes (test duration)..."
        NGROK_URL=$(cat ngrok-url.txt)
        echo "🌐 Dashboard URL: $NGROK_URL"
        
        # Keep alive for 30 minutes for testing
        for i in {1..30}; do
          echo "⏱️ Demo running... ${i}/30 minutes"
          sleep 60
          
          # Periodic health check
          if [ $((i % 5)) -eq 0 ]; then
            curl -s -o /dev/null -w "%{http_code}" "$NGROK_URL" | grep -q "200" && echo "✅ Still accessible" || echo "⚠️ Connectivity check"
          fi
        done
    
    - name: Cleanup
      if: always()
      run: |
        echo "🧹 Cleaning up processes..."
        
        # Kill processes
        [ -f backend.pid ] && kill $(cat backend.pid) 2>/dev/null || true
        [ -f frontend.pid ] && kill $(cat frontend.pid) 2>/dev/null || true
        [ -f ngrok.pid ] && kill $(cat ngrok.pid) 2>/dev/null || true
        
        echo "✅ Cleanup completed"
    
    - uses: actions/upload-artifact@v4
      if: always()
      with:
        name: ngrok-demo-results-fixed
        path: |
          ngrok-live-demo-report.html
          ngrok.log
          ngrok-url.txt
          backend.log
          frontend.log
        retention-days: 30